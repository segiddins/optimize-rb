fixture: polynomial.rb
entry_setup: poly = Polynomial.new
entry_call: if SCALE == 6 then poly.compute(42) else poly.compute(0) end
walkthrough:
  - inlining
  - const_fold_tier2
  - const_fold
  - identity_elim
  - dead_branch_fold
